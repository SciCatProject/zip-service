<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="css/zipping.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://cdn.rawgit.com/kimmobrunfeldt/progressbar.js/0.5.6/dist/progressbar.js"></script>
    <script>
      $( document ).ready(function() {
        var circle = new ProgressBar.Circle('#progressbar', {
          strokeWidth: 20,
          easing: 'easeInOut',
          duration: 1400,
          color: '#3261ad',
          trailColor: '#eee',
          trailWidth: 20,
          svgStyle: null
        });
        var file = '<%= file %>';
        var total = <%= total %>;
        iId = setInterval(function() {poll(circle, total, file)}, 1000);
        $( "#status" ).html("0/" +  total);
      });

      function poll(circle, total, file){
        $.get( "/zip/polling/" + file ).done(function( data ){
          if (data && data.startsWith("/download")){
            clearInterval(iId);
            circle.animate(1);
            $( "#status" ).html("<a target='_blank' href='" + data + "'>Download</a>");
            $( "#title" ).html("Done!");
            
          }else{
            circle.animate(parseFloat(data) / total);
            $( "#status" ).html(Math.floor(parseFloat(data)) + "/" +  total);
          }
          
        });
      }

    </script>
</head>
<body>
  <div class="zipping">
   <div id="title">Please wait, the files are being zipped...</div>
    <div  style="width: 100px;  margin: 20px auto;" id="progressbar"></div>
    <div id="status"></div>
  </div>
</body>
</html>